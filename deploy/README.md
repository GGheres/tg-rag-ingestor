# Deploy Folder

`docker-compose.yml` is currently in repository root for local developer experience.

This folder is reserved for future deployment manifests (staging/prod compose, Kubernetes, Helm, etc.).
